// // let emni: any;
// // emni = "Lorem is same ";
// // (emni as string).length;

// // let emni: any;
// // emni = 22222.0524;
// // (emni as number).toFixed(2);

// function kgTogrem(param: string | number): string | number | undefined {
//   if (typeof param === "string") {
//     const value = parseFloat(param) * 1000;
//     return value;
//   }
//   if (typeof param === "number") {
//     const value2 = param * 1000;
//     return value2;
//   }
// }

// const resultTobeNUmber = kgTogrem(1000) as number;
// const restulToString = kgTogrem("2021") as string;
